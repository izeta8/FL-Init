Now:
    - When the user doesnt select "Separate Stems", in the progress dialog only show the "Download" gauge. Currently there are visible also the stems gauges (vocal, bass, drums, others) 
    - When the full process (download if separate stems is uncheck or stem separation if separate stems is checked) is completed put all the gauges in green. currently only the last one is green.

Later:
- Make script executing faster.
- Create a pipeline to update depedencies into the python embed
